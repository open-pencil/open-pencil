# CLAUDE.md

This project keeps its agent instructions in `AGENTS.md`. Read it first.

@AGENTS.md

Domain-specific rules live in nested `AGENTS.md` files next to the code they govern
(`src/`, `desktop/`, and each `packages/*`). The root file lists them; read the one
closest to the code you are changing before making edits there.
