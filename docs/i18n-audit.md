# View i18n hardcode audit

Auto-generated by `bun scripts/i18n/audit-views.ts`. Counts are heuristic candidates inside <template>, not exact.

| File | Hardcoded candidates | First samples |
|---|---|---|
| BoardsView.vue | 9 | `Create a personal board or attach it to a team workspace without leaving Inkly.` / `Team boards can be created only in teams you own.` / `Search by board name or reopen a recently edited board.` / `Loading boards…` / `Create your first board to start the invite and team sharing flows.` / `This permanently removes the board and its invitation links.` / `Delete board` / `Board name` / `Search boards` |
| AccountView.vue | 6 | `Inkly works without an account. Sign in only if you want to migrate your anonymous boards` / `Loading account…` / `If Google OAuth is not configured in this environment, the button will report that it` / `You will return to anonymous mode on this device.` / `Your boards remain available. Sign in again to restore account notifications and teams.` / `Log out` |
| BoardSettingsView.vue | 6 | `Back to boards` / `Review invitation links and the anonymous collaborators that have accepted them.` / `Open board` / `New invite` / `Loading board settings…` / `No invitation links yet.` |
| NotificationsView.vue | 6 | `Track board invitations, workspace access, and mentions in one place.` / `Mark all read` / `Loading notifications…` / `Notifications are available only for signed-in users.` / `Invitations and mentions will show up here once they arrive.` / `Mark read` |
| TeamsView.vue | 6 | `Share boards through a team workspace and manage roles in one place.` / `New team` / `Loading teams…` / `Create a workspace to share boards with editors and viewers.` / `Team owners can attach boards and manage member roles.` / `Design Ops` |
| TeamDetailView.vue | 4 | `Back to teams` / `Invite member` / `Loading team…` / `No boards attached to this team yet.` |
| TeamSettingsView.vue | 4 | `Back to team` / `Loading settings…` / `Only the team owner can change settings.` / `Deleting the team removes memberships and returns team boards to personal ownership.` |
| DashboardView.vue | 3 | `formatTemplate(dashboard.customize.dragRowAria, { section: sectionLabel(section.id) })` / `formatTemplate(dashboard.customize.dragHandleAria, { section: sectionLabel(section.id) })` / `isPinned(board.id) ? dashboard.pinAria.unpin : dashboard.pinAria.pin` |
| AdminView.vue | 1 | `formatTemplate(admin.boardsTab.selectRowAria, { name: board.name })` |
| EditorView.vue | 0 | — |
| InviteRedirectView.vue | 0 | — |

## Next i18n migration priorities

- BoardsView.vue — 9 candidates
- AccountView.vue — 6 candidates
- BoardSettingsView.vue — 6 candidates
