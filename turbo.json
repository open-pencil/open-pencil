{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "stream",
  "concurrency": "50%",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "check": {
      "dependsOn": ["^check"]
    }
  }
}
